sudo sudo
yum update

yum install httpd

yum install php70 php-devel php-mbstring php-pdo php-gd

// PHP Version 確認
php --version

php.iniを編集
vim /etc/php.ini

//編輯內容
mbstring.internal_encoding = UTF-8
mbstring.http_input = UTF-8

yum install mysql57 mysql57-server

mkdir /要放/file/的地方/千萬不要是/自己的root

groupadd -g 10000 edit
usermod -a -G edit 戶名
usermod -a -G edit apache
sudo chown 戶名:edit “資料夾”
sudo chmod 775 “資料夾”

vim /etc/httpd/conf/httpd.conf

Document Root "資料夾，最後面不要/"

<Directory "/var/www/html"> // 看哪個資料夾
    AllowOverride All
</Directory>

yum reboot

service mysqld start
service httpd start



mysql -u root -p

CREATE USER 'jgwcuk'@'localhost' IDENTIFIED BY 'YOURPASSWORD';
GRANT ALL PRIVILEGES ON jgwc.* TO 'jgwc'@'localhost' WITH GRANT OPTION;

